import { DirectivdDirective } from './directivd.directive';

describe('DirectivdDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivdDirective();
    expect(directive).toBeTruthy();
  });
});
